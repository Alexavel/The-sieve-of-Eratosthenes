#Выписываем все целые числа от 0 до заданной границы
n = int(input('Введите N: '))
a = [i for i in range(n + 1)]
#Вычеркиваем 0 и 1 как непростые числа
a[1] = 0
#Устанавливаем значение переменной i, равное 2
i = 2
#Пока i меньше указанного числа, делать
while i <= n:
  #Вычеркиваем все числа, кратные i, но не его само
    if a[i] != 0:
        j = i + i
        while j <= n :
            a[j] = 0
            j = j + i
#Устанавливаем значение i, равное следующему невычеркнутому числу
    i += 1
# Избавляемся от всех нулей в списке
a = [i for i in a if i != 0]

#Выводим все числа, оставшиеся незачеркнутыми
print(a)
